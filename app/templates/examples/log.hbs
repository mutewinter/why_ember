<header>
  <h4>Log Messages</h4>
  <a {{action expand target="view"}} href="#" class="pull-right">
    {{#if view.isExpanded}}
      shrink
    {{else}}
      expand
    {{/if}}
  </a>
</header>
<pre class="log-area">
{{view.logMessagesText}}
</pre>
