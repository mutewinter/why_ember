# Public: All controllers must be loaded explicitly here so they are available
# when the application is loaded.

require 'controllers/application'

require 'controllers/slides'
require 'controllers/slide'
